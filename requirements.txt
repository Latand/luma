# Studio pipeline (the trainer HTML itself needs nothing). Python 3.11 recommended.
# PyTorch: pick the wheel index for your GPU, e.g. --index-url https://download.pytorch.org/whl/cu128 (CUDA) or /cpu
torch==2.10.0
torchaudio==2.10.0
demucs==4.0.1
torchcrepe==0.0.24
librosa==0.11.0
numpy>=1.26,<3
scipy>=1.12,<2
soundfile>=0.12,<1
